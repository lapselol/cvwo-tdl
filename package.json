{
  "name": "just-do-list",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "build": "yarn --cwd tdl_api_user install && yarn --cwd tdl_api_user build",
    "deploy": "cp -a tdl_api_user/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}