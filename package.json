{
  "name": "just-do-list",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "npm": "8.1.2"
  },
  "scripts": {
    "build": "npm --prefix tdl_api_user run install && npm --prefix tdl_api_user run build",
    "deploy": "cp -a tdl_api_user/build/. public/",
    "heroku-postbuild": "npm build && npm deploy"
  }
}